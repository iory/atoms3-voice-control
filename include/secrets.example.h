#pragma once

// Copy to include/secrets.h (git-ignored) and fill in.
// The iPhone must be on the same network and able to reach the internet
// (it resolves *.local-ip.sh through public DNS).
#define WIFI_SSID "your-ssid"
#define WIFI_PASSWORD "your-password"
